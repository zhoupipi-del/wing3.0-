# Wings 3.0 Backend Dependencies
# 安装: pip install -r requirements.txt

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy[asyncio]==2.0.36
aiomysql==0.2.0
pymysql==1.1.1

# Auth
PyJWT==2.10.1
python-multipart==0.0.18
# 密码哈希（core/services.py 运行时代码 import bcrypt；此前漏声明，CI 干净环境缺包）
bcrypt>=4.1

# Validation
pydantic==2.10.3

# LLM (from existing system)
httpx==0.28.1

# Redis (分布式事件总线 pub/sub)
redis[hiredis]>=5.0.0

# SSE (Server-Sent Events — CEP复合预警实时泵站)
sse-starlette>=2.0.0

# Dev
watchfiles==1.0.0
