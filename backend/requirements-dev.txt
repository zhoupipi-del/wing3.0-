# Wings 3.0 Backend Test/Dev Dependencies
# 安装: pip install -r requirements-dev.txt
# CI 必须依赖此文件拉取 pytest，禁止在 workflow 临时 pip install pytest。

# 测试运行器
pytest>=8.0

# 异步测试支持：backend/tests 用 @pytest.mark.asyncio
pytest-asyncio>=0.24

# HTTP 测试客户端（FastAPI 接口测试用）
httpx>=0.27

# 测试脚本依赖：tests/test_explainer_scenarios.py、tests/inject_research_data.py
# 用 requests 调本地服务做端到端场景校验。生产代码不依赖 requests，故仅放 dev。
requests>=2.31
